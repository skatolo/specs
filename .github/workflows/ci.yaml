name: CI

on:
    pull_request:
        branches:
        - '*'
    push:
        branches:
        - '*'
    workflow_dispatch:

jobs:
    specs:
        name: specs
        runs-on: buildjet-64vcpu-ubuntu-2204-arm
        steps:
        - uses: actions/checkout@v1
        - name: Check specs
          run: |
            ./specs.sh

