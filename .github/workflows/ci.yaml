name: CI

on:
  pull_request:
    branches:
      - '*'
  push:
    branches:
      - master

jobs:

  specs:
    name: specs
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v1

      - name: Check specs
        run: |
          ./specs.sh
